'use client';

import { useState } from 'react';

const ClientOnly = () => {
    const [hasMounted, setHasMounted] = useState(false);
    return (
        <div>

        </div>
    );
}
 
export default ClientOnly;